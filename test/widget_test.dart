// Basic test — full app pump requires Supabase (see integration_test or mocks).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(2 + 2, 4);
  });
}
